#pragma once
#include <vector>

using namespace std;

class CourseSchedule_3 {
public:
    int scheduleCourse(vector<vector<int>>& courses) {
        for (auto& course : courses)
        {
            if (course)
        }

        size_t courseCount = courses.size();
        size_t count = 0;
        int startTime = 0;
        int totalTime = 0;

        for (const auto& course : courses)
        {
            if (cour)
        }
    }
};